#include <bits/stdc++.h>
using namespace std;
int fact(int x);
int main()
{
    int x;
    cin >> x;
    fact(x);
	return 0;
}
int fact(int x)
{
    for(int i = 1; i <= x; i++)
    {
        i == x ? cout << i : cout << i << " ";
    }
    return 0;
}
