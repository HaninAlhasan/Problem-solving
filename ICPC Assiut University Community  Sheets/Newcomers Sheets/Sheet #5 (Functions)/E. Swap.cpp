#include <bits/stdc++.h>
using namespace std;
bool swap(int x,int y);
int main()
{
    int x,y;
    cin >> x >> y;
    swap(x,y);
    
	return 0;
}
bool swap(int x,int y)
{
    cout << y << " " <<  x << endl;
    return 0;
}
